#### synchronously ( сombinational ) :
```
always @(*edge clk) | *( pos/neg )
    data <= value;
```
* tact
* memory
* relation only from the entrance + from the previous state
#### asynchronously ( sequential ) :
```
wire sum;
assign sum = a ^ b;
```
```
reg sum;
always @(*) 
    sum = a ^ b;
```
* no tact
* no memory
* relation only from the entrance

#### assignment
``` 
<=[non-blocking assignment]         =[blocking assignment] ( ) 
* at the end of the signal tact     * straightaway
```